# .env.example - Template de variables de entorno

# === DATABASE CONFIGURATION ===
DB_HOST=localhost
DB_PORT=5432
DB_NAME=elderly_survey
DB_USER=postgres
DB_PASSWORD=your_postgres_password_here

# === REDIS CONFIGURATION ===
REDIS_HOST=localhost
REDIS_PORT=6379
CELERY_BROKER_URL=redis://localhost:6379/0

# === WHATSAPP BUSINESS API ===
WHATSAPP_API_TOKEN=your_whatsapp_business_token_here
WHATSAPP_PHONE_NUMBER_ID=your_phone_number_id_here
WHATSAPP_VERIFY_TOKEN=your_webhook_verify_token_here

# === GOOGLE GEMINI AI ===
GEMINI_API_KEY=your_gemini_api_key_here

# === SECURITY ===
SECRET_KEY=your_secret_key_for_production_here

# === AWS CONFIGURATION (for production) ===
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
AWS_REGION=us-east-1
AWS_S3_BUCKET=your-s3-bucket-name

# === PRODUCTION DATABASE (AWS RDS) ===
AWS_RDS_HOST=your-rds-endpoint.region.rds.amazonaws.com
AWS_RDS_PORT=5432
AWS_RDS_NAME=elderlysurvey
AWS_RDS_USER=postgres
AWS_RDS_PASSWORD=your_rds_password

# === PRODUCTION REDIS (AWS ELASTICACHE) ===
AWS_REDIS_HOST=your-redis-endpoint.cache.amazonaws.com
AWS_REDIS_PORT=6379
AWS_REDIS_PASSWORD=your_redis_password

# === APPLICATION SETTINGS ===
ENVIRONMENT=development
DEBUG=true
LOG_LEVEL=INFO

# === RATE LIMITING ===
RATE_LIMIT_PER_MINUTE=60
RATE_LIMIT_PER_HOUR=1000

# === AUDIO PROCESSING ===
VOSK_MODEL_PATH=vosk-model-small-es-0.42
AUDIO_MAX_DURATION=300
AUDIO_MAX_SIZE_MB=25

# === MONITORING (opcional) ===
SENTRY_DSN=your_sentry_dsn_for_error_tracking
PROMETHEUS_PORT=8000

# === INSTRUCTIONS ===
# 1. Copiar este archivo a .env
# 2. Reemplazar todos los valores "your_*_here" con credenciales reales
# 3. NUNCA commitear el archivo .env con credenciales reales
# 4. Para producción, usar variables de entorno del sistema o secrets manager